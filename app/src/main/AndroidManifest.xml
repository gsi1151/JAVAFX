<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="javafx.utng.edu.mx.javafx">

    <uses-sdk
        android:minSdkVersion="9"
        android:targetSdkVersion="18"
        tools:replace="targetSdkVersion" />

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality. 
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup="true"
       android:icon="@drawable/aja"
        android:label="@string/name_app"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".StartActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".LoginActivity">


        </activity>
        <activity android:name=".answers.jpa.QuestionJPAActivity" />
        <activity android:name=".answers.entities.QuestionEntitiesActivity" />
        <activity android:name=".answers.persisten.QuestionPersistenActivity" />
        <activity android:name=".answers.primarykey.QuestionPrimaryKeyActivity" />
        <activity android:name=".answers.relation.QuestionRelationActivity" />
        <activity android:name=".GamesActivity" />
        <activity android:name=".PreguntaEjemplo" />
        <activity android:name=".estrategia.Manager" />
        <activity android:name=".activity.JPAActivityOne" />
        <activity android:name=".activity.PersistenActivityTwo" />
        <activity android:name=".activity.RelationActivityThree" />
        <activity android:name=".activity.EntitiesActivityFour" />
        <activity android:name=".activity.PrimaryKeyActivityFive" />
        <activity android:name=".video.VideoActivityUno" />
        <activity android:name=".video.VideoActivityDos" />
        <activity android:name=".video.VideoActivityTres" />
        <activity android:name=".video.VideoActivityCuatro" />
        <activity android:name=".video.VideoActivityCinco" />
        <activity android:name=".video.VideoActivityUtng" />
        <activity android:name=".SignUp" />
        <activity android:name=".TemarioActivity" />
        <activity android:name=".pdfgrafics.GrapicsActivity" />
        <activity android:name=".Mail" />
        <activity android:name=".MusicaActivity" />
        <activity android:name=".galeria.GaleryJPA" />
        <activity android:name=".galeria.GaleryUTNG" />
        <activity android:name=".TemeActivity" />
        <activity android:name=".Calculadora" />
        <activity android:name=".agenda.AcercaDeAg" />
        <activity android:name=".agenda.AgendaFinalActivity" />
        <activity android:name=".agenda.NoteEdit" />
        <activity android:name=".agenda.Notepad" />

        <activity android:name=".gato.JuegoCpu" />
        <activity android:name=".gato.AcercaDe" />
        <activity android:name=".JuegoNumero"/>
        <activity android:name=".StreamingActivity"/>

        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" />

        <activity
            android:name=".acercade.AcercaDeScrollingActivity"
            android:label="@string/title_activity_acerca_de_scrolling"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".SettingsActivity"
            android:label="@string/title_activity_settings" />

        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/. 
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".MapsActivity"
            android:label="@string/title_activity_maps"></activity>
    </application>

</manifest>
